[package]
name = "glib-variant-regression"
version = "0.1.0"
edition = "2021"
publish = false

# A test-only workspace member; uses the vendored crate and shares the application's lockfile.
[target.'cfg(target_os = "linux")'.dependencies]
glib = { path = "../../../vendor/glib-0.18.5", default-features = false }
